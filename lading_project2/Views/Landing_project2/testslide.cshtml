@{
    ViewBag.Title = "indexsosanh";
    Layout = "~/Views/Shared/_layout_Desktop.cshtml";
}
@section header {
    <style>
        .slider {
            width: 300px;
            height: 200px;
            overflow-x: scroll;
            white-space: nowrap;
            -webkit-overflow-scrolling: touch; /* Cho các trình duyệt dựa trên WebKit (ví dụ: Safari trên iOS) */
            scroll-behavior: smooth; /* Cuộn mượt */
        }
        .slide {
            display: inline-block;
            width: 300px;
            height: 200px;
            background-color: #ccc;
            margin-right: 10px;
        }
    </style>
}

    <div class="slider">
        <div class="slide">Slide 1</div>
        <div class="slide">Slide 2</div>
        <div class="slide">Slide 3</div>
        <div class="slide">Slide 4</div>
        <!-- Thêm các slide khác ở đây -->
    </div>
